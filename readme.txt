To build the library, run argoenv.sh with the correct Argo folder set up (it is currently set up for the project snic2017-7-43 on Uppmax) and then build the starpu library using make.

To run cholesky, just submit the job using the SLURM command
"sbatch devtest_cholesky2.sh"
